units metal
atom_style atomic
boundary p p p

# Create a tiny Si box
lattice diamond 5.43
region box block 0 1 0 1 0 1
create_box 1 box
create_atoms 1 box
mass 1 28.0855

neighbor 2.0 bin
neigh_modify delay 0 every 1 check yes

# Load NequIP model via ML-IAP; use container-visible path (bound to /work)
pair_style mliap unified /work/output.nequip.lmp.pt 0
pair_coeff * * Si

fix nve all nve

run 10


